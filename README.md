[This is a stub: Integration with jQuery sortable, which seems obvious, has caused design quandries that make me wonder if this can be a proper plugin.]

jquery-shuffle
================

Simple jQuery plugin to shuffle DOM elements
